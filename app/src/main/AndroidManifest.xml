<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tku.accountingsd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.reminders.addReminderActivity" />
        <activity android:name=".ui.reminders.UpdateReminderActivity" />
        <activity android:name=".ui.depositTarget.depositTargetActivity" />
        <activity android:name=".ui.account.accountActivity" />
        <activity android:name=".ui.newRecord.UpdateRecordActivity"></activity>
    </application>

</manifest>